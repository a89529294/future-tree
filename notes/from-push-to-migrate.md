## Generate initial migration from current DB state

```bash
npx drizzle-kit generate --name initial
npx drizzle-kit migrate
```

## 3 step process

### change your schema

### generate migration

```bash
npx drizzle-kit generate --name add_user_verification
```

### run migration

```bash
npx drizzle-kit migrate
```
